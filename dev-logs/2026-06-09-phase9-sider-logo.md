# Dev Log — 2026-06-09 (Phase 9: Sider Logo + 面包屑)

## 摘要

A 档投票完成：**Sider Logo 重设计 + Header 面包屑**。

- Sider 顶部：BoxPlotOutlined 蓝色图标 + "纸箱"（去掉 Header 品牌名重复）
- Header 左侧：Breadcrumb 面包屑（首页 + 当前页面名称）
- Header 右侧：保留用户下拉菜单

## 修改文件

- `apps/web/src/components/Layout.tsx` — 重写（5.5 KB → 6.6 KB）

## 设计改动

### Sider 顶部（之前 → 之后）

**之前**：
```
┌──────────────────┐
│     纸箱 ERP      │  ← 纯文字，64px 高
└──────────────────┘
```

**之后**：
```
┌──────────────────┐
│  📦 纸箱          │  ← 蓝色 BoxPlotOutlined + 深灰文字
└──────────────────┘
```

- 图标：`BoxPlotOutlined`（蓝色 #1677ff）
- 文字："纸箱"（17px, 600 weight, #262626, letterSpacing: 1）
- 点击跳转首页（cursor: pointer）

### Header（之前 → 之后）

**之前**：
```
┌────────────────────────────────────────────┐
│ 纸箱 ERP                    管理员 ▼       │  ← 品牌名重复
└────────────────────────────────────────────┘
```

**之后**：
```
┌────────────────────────────────────────────┐
│ 🏠 首页                          管理员 ▼  │  ← 面包屑
└────────────────────────────────────────────┘
```

非首页时：
```
┌────────────────────────────────────────────┐
│ 🏠 首页 > 订单管理                 管理员 ▼ │
└────────────────────────────────────────────┘
```

### 面包屑逻辑

```typescript
const PAGE_NAMES = {
  '/': '概览',
  '/products': '产品管理',
  '/orders': '订单管理',
  // ... 13 个页面
};

const breadcrumbItems = [
  { title: <><HomeOutlined /> 首页</> },
  ...(location.pathname !== '/' ? [{ title: pageName }] : []),
];
```

## 质量门禁

| 检查 | 结果 |
|------|------|
| vite build | ✅ EXIT 0 |
| 浏览器截图 | ✅ Sider Logo + 面包屑 + Header 品牌名已去 |

## 浏览器验证

snapshot 确认：
- Sider 顶部：`box-plot` 图标 + "纸箱" 文字
- Header 左侧：面包屑 "🏠 首页"
- Header 右侧："管理员" 用户下拉
- 无品牌名重复

## 后续 5 档投票

| 档位 | 建议 |
|------|------|
| **A** | **部署正式服 — 推荐** |
| B | 暂停等反馈 |
| C | 其他（用户指定） |