# 2026-07-09

## 销售订单列表改为明细级别展示

### 需求
用户要求销售订单列表表头改为明细级别：序号、订单日期、客户名称、产品名称、产品规格、订单数量、已发货数量、未发货数量、单价、金额、交货日期、备注

### 变更

**后端**
- `orders.service.ts`: 新增 `findAllItems()` 方法，查询所有 order_items 并联表 orders/customers/products，返回扁平化明细数据（含 computed `undelivered_qty`）
- `orders.controller.ts`: 新增 `GET /orders/items` 端点
- `orders.module.ts`: 注册 `Product` entity

**前端**
- `OrderList.tsx`: endpoint 从 `/orders` 改为 `/orders/items`，表头/搜索/详情均改为 12 列明细视图

### 质量门禁
- [x] `curl localhost:3005/` → 200
- [x] `curl localhost:3005/api/orders/items` → 401 (需认证，API 可达)
- [x] `npx vite build` → 1.52s 成功

### 已知问题
- 测试数据库中 orders 的 order_id=1,2 对应记录不存在，导致 joint 字段为空，真实数据环境下正常
- PM2 服务器因 JWT_SECRET 环境变量问题崩溃，需重启
