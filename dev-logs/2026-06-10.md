# 2026-06-10 开发日志

## 上午：纸箱 ERP vs 旧系统对比 + P0 补全

### 旧 vs 新 ERP 对比（/data/erp-data/erp-system/ vs /root/workspace/paperbox-erp/）

用 browser_vision 实际访问 4 个旧系统核心页面，结合静态代码分析得出修正版对比：

| Page | 旧版实际 UI | 新版实际代码 | 真实差距 |
|------|-----------|-------------|---------|
| 销售订单 | 6 列 + 详情弹窗 + 状态行内下拉 | 11 列 + 编辑 Modal | 中（缺产品明细行）|
| 采购单 | 6 列 + 详情 + 状态下拉 | 8 列只读 | **严重**（无任何操作）|
| 送货单 | 9 列 + 发货按钮 | 5 列只读 | **严重**（无任何操作）|
| 工单管理 | 9 列 + 开工/完工 | 18 列 + 排产/开始/完工/取消 | 新版更完整 |

**重要修正**：之前静态分析夸大旧版 UI 复杂度，实际旧版核心 4 页都比较简洁。

### P0 任务：Purchases.tsx 补全

| 指标 | 旧 | 新 |
|------|---|---|
| 代码行数 | 44 行（只读表）| 672 行（完整 CRUD + 业务闭环）|
| 业务按钮 | 0 | 8 个（详情/审批/驳回/入库/打印/编辑/删除/取消）|
| 弹窗 | 0 | 3 个（新建/编辑/详情）|
| KPI 卡片 | 0 | 5 个（总采购单/待审批/已审批/已入库/总金额）|
| 筛选 | 1（搜索）| 3（搜索/状态/供应商）|
| 导出 | 0 | CSV 导出（带 BOM 支持 Excel 打开）|
| 状态色 | 旧版硬编码 | 统一调用 getStatusColor() |
| 后端端点 | 1 | 11 个全部接入 |

**后端 API 完整**（11 个）：列表/详情/创建/更新/删除/审批/收货/取消/生成单号/更新单号/明细。
**前端之前只用了 1 个 GET**（findAll）。今天补全 10 个端点的前端调用。

### 验证

- typecheck: 0 errors
- vite build: ✓ built in 7.61s, Purchases chunk 17.52 kB
- 部署: nginx reload OK
- 浏览器访问 /purchases: 7 条采购单全部正常加载
- KPI 实时统计: 总采购单 7 / 待审批 2 / 已审批 2 / 已入库 0 / 总金额 ¥2,700.00
- 操作按钮按状态动态显示（待审批 8 个，已审批 5 个，已出单 4 个）
- 详情弹窗：8 字段基本信息 + 8 列明细表 + 合计行（数量小计 + 金额小计）
- 新建弹窗：3 字段顶部 + 备注 + 采购明细（可增删行）+ 提交
- Console: 0 errors, 0 messages

### 视觉评估（browser_vision 7/7 PASS）

- 状态色映射正确（待审批=橙，已审批=蓝，已入库=绿，已出单=灰）
- 操作列按钮按状态动态显示（业务闭环严谨）
- KPI 卡片 5 色差异化（品牌色/橙/蓝/绿/红）
- 弹窗布局合理（详情 840px，新建 840px）
- 表格 9 列 + scroll.x 1400 + 操作列 fixed right
- 品牌色一致（深蓝 #2c5282 主题）

### 已知 P1 改进点（vision 反馈，未修）

1. 新建弹窗明细行字段拥挤（col span 6/4/3/2/2/3/3 偏紧）→ 改为 1000px 宽度 + 调整比例
2. 新建弹窗缺「金额」自动计算列（当前依赖后端算，前端不可见）→ Form.List 加 Form.Item shouldUpdate
3. 新建弹窗缺「税率」字段 → 后端 Purchase entity 暂不支持，P2
4. KPI 缺趋势对比（近 7 天 vs 前 7 天）→ P2
5. 金额列 ¥0.00 灰显 → P2

### 下一步

□ P0-2: Deliveries.tsx 41 → ~300 行（同样模式：KPI/CRUD/详情/发货/签收/打印）
□ P0-3: Orders.tsx 详情弹窗 + 产品明细行
□ P0-4: Receivables.tsx + Payables.tsx 2 个新 page

## 5 档投票

| 档 | 建议 |
|----|------|
| A | 继续 P0-2 Deliveries（按相同模式 1-2 小时搞定） |
| B | 先做视觉打磨（修 P1 五项，每项 5-10 分钟）|
| C | 跳到 P0-3 Orders 详情（涉及产品明细行，更复杂）|
| D | 暂停，先把这次 P0 推到正式服 |
| E | 暂停，等用户反馈 |

**当前选择**：A（已自动启动，等用户打断）


## 中午续：P0-3 销售订单详情与产品明细补全

- `Orders.tsx` 140 → 596 行：补 KPI、筛选、详情弹窗、产品明细 Form.List、CSV 导出、状态行内切换。
- `types/api.ts`：补全 Order 纸箱行业字段（面纸/中纸/印刷/表面处理/刀模/金额核算）。
- 验证：typecheck 0 errors、vite build 7.85s、nginx reload OK、浏览器 `/orders` 23 条数据、详情弹窗产品明细合计正确、新建弹窗明细字段完整、console 0 errors、图标审计 PASS。
- 注意：`Table.Summary.Cell` 不能传 `style`；行内生成发货要先拉详情 items，不能依赖全局 detail state。


## 中午续 2：P0-4 应收/应付新页面补全

- 新增 `FinanceRecordsPage.tsx` 共用业务页，新增 `Receivables.tsx` / `Payables.tsx`。
- 改造 `Finance.tsx` 为财务总览 + 应收/应付入口。
- 接入 `/receivables` `/payables` 路由和菜单权限，补 FinanceRecord 类型与财务状态色。
- 验证：typecheck 0 errors、图标审计 PASS、vite build 7.61s、nginx reload OK。
- 浏览器：应收 18 条/KPI 正常/详情与新建弹窗正常；应付空态与新建弹窗正常；财务总览正常；console 0 errors。
- P0 旧系统核心业务页缺口（采购/发货/订单详情/应收/应付）已全部补完。


## 下午：P0 业务页全链路回归

- 后端授权 GET 接口健康检查：15/15 PASS（products/orders/work_orders/purchases/deliveries/finance_records 等）。裸 curl 401 为 JWT 安全预期。
- 前端 12 页路由回归：概览/产品/订单/工单/采购/委外/仓库/发货/对账/财务/应收/应付全部非白屏，核心表格和按钮渲染正常。
- 浏览器 console：0 errors / 0 messages。
- browser_vision：无 P0 阻断；发现 P1：Orders 操作列拥挤、Purchases 单号换行+按钮拥挤、Deliveries `ID:null`、Receivables 到期日截断+按钮拥挤。
- 产出报告：`docs/regression-report-2026-06-10-p0-pages.md`。

## 下午：P1 回归问题修复

- 修复发货管理关联数据显示 `ID:null`：表格、详情、CSV 导出均改为 `未关联` 友好兜底。
- 修复采购管理采购单号换行：单号列加宽，使用 monospace + nowrap + Tooltip。
- 修复采购管理操作列拥挤：低频/危险操作收纳到 `更多` 下拉，删除改 Modal.confirm 二次确认。
- 修复订单管理金额换行和操作列拥挤：金额列 nowrap，打印/删除收纳到 `更多`。
- 修复应收/应付财务页到期日截断与操作列拥挤：单号/日期 nowrap，冲正/编辑/删除收纳到 `更多`。
- 清理不再使用的 Popconfirm import。
- 验证：tsc PASS、AntD 图标审计 PASS、vite build PASS、nginx reload PASS、浏览器 console 0 errors。
- 视觉抽检：采购/发货/应收 P1 修复 PASS；剩余 P2 为发货操作列仍可进一步收纳。

## 下午：P2 表格操作列打磨（仅测试服）

- 按用户要求：继续修 P2，明确不做正式服同步。
- 发货管理操作列：待发货行从 `详情 / 发货 / 打印 / 编辑 / 删除` 收纳为 `详情 / 发货 / 更多`；已发货行为 `详情 / 签收 / 更多`；已签收行为 `详情 / 更多`。
- 发货管理 `更多` 使用 Dropdown + `placement=bottomRight`，打印/编辑/删除在浮层内，删除使用 Modal.confirm 二次确认。
- 财务通用页（应收/应付）操作列宽度 190 → 210，并给 Dropdown 设置 `placement=bottomRight` + 按钮右侧 padding，避免靠右贴边。
- 验证：tsc PASS、AntD 图标审计 PASS、vite build PASS、nginx reload PASS。
- 浏览器验证：发货/应收/应付页面正常，console 0 errors；发货与应收 browser_vision PASS；应付空态 browser_vision PASS。

