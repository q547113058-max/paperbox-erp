# 开发日志 - 2026-06-29

## 会话

- 任务：代码回退到昨天状态 + 品牌更名 + GitHub 推送
- 需求来源：用户指令
- 当前阶段：交付

## 已完成

- 识别并回退今日（6/29）非会话产生的改动：删除来历不明的 `server/` / `frontend/` 目录，回退 `ecosystem.config.cjs` / `package-lock.json`
- 品牌更名：丰晟达 → 合升纸品（Login.tsx / BrandLogo.tsx / Layout.tsx / index.html 共 7 处）
- 重建前端 + 重启后端，验证 HTTP 200 / API 正常
- 推送至 GitHub main 分支，删除 deploy 分支
- 清理不该入库的文件：DB 文件 + .hermes/plans/，更新 .gitignore

## 变更文件

- `apps/web/index.html` — 标题改为"合升纸品 ERP"
- `apps/web/src/pages/Login.tsx` — 3 处品牌文字
- `apps/web/src/components/BrandLogo.tsx` — 默认文字 + 注释
- `apps/web/src/components/Layout.tsx` — 侧边栏底部公司名
- `.gitignore` — 新增 *.db / .hermes/plans/ 忽略规则
- `ecosystem.config.cjs` — 回退至 git 版本
- `package-lock.json` — 回退至 git 版本
- 删除 `server/` / `frontend/` 目录（非会话产生）

## 决策

- `server/` 和 `frontend/` 目录非任何 Hermes 会话产生，直接删除
- 品牌名从"丰晟达食品"改为"合升纸品"
- 保留所有 6/26-27 会话产生的改动（EX 式单据、产品字段调整等）
- DB 文件不应入库，添加至 .gitignore 并清理历史

## 验证

- 前端 HTTP 200 ✓
- API `/api/products` 正常返回 ✓
- 构建成功（npx vite build）✓

## 未解决问题

- 无

## 下一步待办

- 无
